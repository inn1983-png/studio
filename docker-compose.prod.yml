version: '3.8'

# Txtovideo Studio - 生产环境完整部署
# 包含所有服务:前端、后端、Worker、数据库、缓存、存储

services:
  # ==========================================
  # 基础设施服务
  # ==========================================

  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: txtovideo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-txtovideo_studio}
      POSTGRES_USER: ${POSTGRES_USER:-txtovideo_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-txtovideo_password}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - txtovideo-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-txtovideo_user} -d ${POSTGRES_DB:-txtovideo_studio}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存和消息队列
  redis:
    image: redis:7-alpine
    container_name: txtovideo-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis_password}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    networks:
      - txtovideo-network
    healthcheck:
      test: [ "CMD", "redis-cli", "--raw", "incr", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # MinIO 对象存储
  minio:
    image: minio/minio:latest
    container_name: txtovideo-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    networks:
      - txtovideo-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9000/minio/health/live" ]
      interval: 30s
      timeout: 10s
      retries: 3

  # ==========================================
  # 应用服务
  # ==========================================

  # 后端 API 服务
  backend:
    image: txtovideo/txtovideo-backend:latest
    container_name: txtovideo-backend
    restart: unless-stopped
    environment:
      # 数据库配置
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-txtovideo_user}:${POSTGRES_PASSWORD:-txtovideo_password}@postgres:5432/${POSTGRES_DB:-txtovideo_studio}

      # Redis配置
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0

      # MinIO配置
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin123}
      MINIO_BUCKET: ${MINIO_BUCKET:-txtovideo-studio}
      MINIO_SECURE: "false"

      # 应用配置
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:?JWT_SECRET_KEY must be set}
      API_KEY_ENCRYPTION_KEY: ${API_KEY_ENCRYPTION_KEY:?API_KEY_ENCRYPTION_KEY must be set}
      DEBUG: ${DEBUG:-false}
      ENVIRONMENT: production
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-localhost:9000}

    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - txtovideo-network
    volumes:
      - ./backend/src:/app/src # 挂载源代码，支持热重载
      - ./backend/logs:/app/logs
    command: >
      sh -c "
        alembic upgrade head &&
        uvicorn src.main:app --host 0.0.0.0 --port 8000
      "

  # Celery Worker - 异步任务处理
  celery-worker:
    image: txtovideo/txtovideo-backend:latest
    container_name: txtovideo-celery-worker
    restart: unless-stopped
    environment:
      # 使用与backend相同的环境变量
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-txtovideo_user}:${POSTGRES_PASSWORD:-txtovideo_password}@postgres:5432/${POSTGRES_DB:-txtovideo_studio}
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin123}
      MINIO_BUCKET: ${MINIO_BUCKET:-txtovideo-studio}
      MINIO_SECURE: "false"
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:?JWT_SECRET_KEY must be set}
      API_KEY_ENCRYPTION_KEY: ${API_KEY_ENCRYPTION_KEY:?API_KEY_ENCRYPTION_KEY must be set}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-localhost:9000}
    depends_on:
      - backend
      - redis
    networks:
      - txtovideo-network
    volumes:
      - ./backend/src:/app/src # 挂载源代码，支持热重载
      - ./backend/logs:/app/logs
    command: celery -A src.tasks.app worker --loglevel=info --concurrency=4

  # Celery Beat - 定时任务调度
  celery-beat:
    image: txtovideo/txtovideo-backend:latest
    container_name: txtovideo-celery-beat
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-txtovideo_user}:${POSTGRES_PASSWORD:-txtovideo_password}@postgres:5432/${POSTGRES_DB:-txtovideo_studio}
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:?JWT_SECRET_KEY must be set}
      API_KEY_ENCRYPTION_KEY: ${API_KEY_ENCRYPTION_KEY:?API_KEY_ENCRYPTION_KEY must be set}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-localhost:9000}
    depends_on:
      - backend
      - redis
    networks:
      - txtovideo-network
    volumes:
      - ./backend/src:/app/src # 挂载源代码，支持热重载
      - ./backend/logs:/app/logs
    command: celery -A src.tasks.app beat --loglevel=info

  # 前端服务
  frontend:
    image: txtovideo/txtovideo-frontend:latest
    container_name: txtovideo-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend
    networks:
      - txtovideo-network

# ==========================================
# 网络配置
# ==========================================
networks:
  txtovideo-network:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.223.0/24

# ==========================================
# 数据卷配置
# ==========================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  minio_data:
    driver: local
